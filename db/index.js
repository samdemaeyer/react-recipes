const { recipes } = require('./recipes')
const { recipeDetails } = require('./recipe-details')
const { subRecipeDetails } = require('./sub-recipe-details')

module.exports = {
  recipes,
  recipeDetails,
  subRecipeDetails,
}
